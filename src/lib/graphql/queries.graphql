query HomeBots($pagination: PaginationInput) {
  bots(pagination: $pagination) {
    nodes {
      avatar
      shortDescription
      name
      tags {
        displayName
      }
      id
      guildCount
      votes {
        totalCount
      }
      certified
    }
  }
}

query SingleBot($input: GetBotInput!) {
  getBot(input: $input) {
    avatar
    certified
    description
    website
    supportServer
    status
    shortDescription
    prefix
    github
    guildCount
    id
    importedFrom
    inviteLink
    name
    owners {
      username
      id
      avatar
    }
    tags {
      displayName
      name
    }
  }
}

query SingleBotVote($input: GetBotInput!) {
  getBot(input: $input) {
    avatar
    certified
    id
    name
    votes {
      totalCount
    }
    guildCount
  }
}

query Session {
  me {
    id
    username
    permissions
    avatar
  }
}

query Vanity($input: GetVanityInput!) {
  getVanity(input: $input) {
    id
    targetId
    type
    userId
  }
}

query CanVote($input: BotVoteCreateInput!) {
  canVote(input: $input) {
    canVote
    expires
  }
}

query Webhook($input: GetWebhookInput!) {
  getWebhook(input: $input) {
    id
    events
    payloadFields
    secret
    url
  }
}