overwrite: true
schema: "http://localhost:3001/graphql"
documents: "src/**/*.graphql"
generates:
  src/lib/types/apollo.ts:
    plugins: ["typescript", "typescript-resolvers", "typescript-operations", "typescript-react-apollo"]
  ./graphql.schema.json:
    plugins: ["introspection"]
